# Optimized requirements for Railway deployment
# CPU-only torch, minimal dependencies
# Target: <3GB image size

# ============================================================================
# Core dependencies (minimal set for LightRAG API)
# ============================================================================

# Async framework
aiofiles==23.2.1
asyncio==3.4.3

# Web framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# HTTP client
httpx==0.25.1
httpcore==1.0.2

# Authentication
PyJWT==2.8.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Utilities
psutil==5.9.6
pytz==2023.3
distro==1.8.0
jiter==0.0.5

# Data processing
numpy==1.24.3
networkx==3.2.1

# Embeddings
sentence-transformers==2.2.2

# Storage
hnswlib==0.7.0

# Tokenization
tiktoken==0.5.1

# Logging and colors
ascii_colors==0.1.2

# Excel support (optional, can remove if not needed)
xlsxwriter==3.1.9
openpyxl==3.1.2

# ============================================================================
# ML Dependencies - CPU-only versions
# ============================================================================

# PyTorch CPU-only (200MB vs 2GB with CUDA)
--find-links https://download.pytorch.org/whl/torch_stable.html
torch==2.1.0+cpu

# Transformers with minimal deps
transformers==4.35.0
tokenizers==0.15.0
sentencepiece==0.1.99

# ============================================================================
# Optional: Reduce further by removing these if not needed
# ============================================================================

# Remove if not using:
# - xlsxwriter (Excel export)
# - openpyxl (Excel import)
# - sentence-transformers (if only using Vietnamese embedding)
